"""The forward model: the composed pick-and-place planner and its scenes.

Nothing here fits anything.  `pickplace` is the differentiable IK -> 4-segment
trajopt chain; `scenes` builds the task instances it is solved on.
"""
