experiment: demo_quality
module: iosp.experiments.e2_demo_quality
description: "E2: demo-quality ablation — does recovery track curation, not count?"

defaults: {}

runs:
  - name: ablation

env:
  CUDA_VISIBLE_DEVICES: auto
  XLA_PYTHON_CLIENT_PREALLOCATE: "false"
  XLA_FLAGS: "--xla_disable_hlo_passes=fusion"
  PYTHONUNBUFFERED: "1"
